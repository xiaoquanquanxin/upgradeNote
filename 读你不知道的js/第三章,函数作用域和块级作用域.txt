函数作用域:在函数内部声明的变量可以在这个范围内被访问

隐藏内部实现
最小暴露原则,隐藏部分变量到函数中,比如:设计模块

规避冲突:
1.命名空间:在全局作用域声明一个足够独特的变量,通常是一个对象,所有相关功能都通过这个对象的属性访问.
2.模块管理:session5

函数声明的第一个关键字是function,其他是函数表达式
最大的区别是标识符会绑定在何处

立即执行的匿名函数表达式IIFE
在umd(淘宝的模块)中的应用:
(function(fn){
    fn(参数)
}(function(param){
    执行
}));


块级作用域:
with
try...catch
let关键字
最好显示地创建块级作用域{}


